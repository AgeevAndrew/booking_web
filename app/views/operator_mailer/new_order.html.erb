<h2><%= t('.title', num: @order.num) %></h2>

<%= link_to t('.activate'), accept_order_url(@order.id, token: @order_token.token) %>
<%= link_to t('.cancel'), cancel_order_url(@order.id, token: @order_token.token) %>
